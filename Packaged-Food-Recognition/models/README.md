# Local models

Store OCR model files or weights here locally. Large model files are excluded by `.gitignore`; document the model name, version, and checksum elsewhere before sharing it through approved private storage.
