# Local OCR data

Store product-label images, OCR annotations, and dataset splits here locally. These files are excluded by the repository `.gitignore`; do not commit private or large image datasets.
